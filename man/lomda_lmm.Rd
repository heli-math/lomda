% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stages.R
\name{lomda_lmm}
\alias{lomda_lmm}
\title{Stage 2: LMM on PC scores}
\usage{
lomda_lmm(scores_df, n_pc = 3, time_col = "visit", id_col = "ID", REML = FALSE)
}
\arguments{
\item{scores_df}{A data frame containing at least \code{ID}, the time
variable, and columns named \code{PC1}, \code{PC2}, etc.}

\item{n_pc}{Integer. Number of PC columns to model.}

\item{time_col}{Character. Time variable used as the fixed effect in the
LMM. Defaults to \code{"visit"}.}

\item{id_col}{Character. Subject identifier column used for the random
intercept. Defaults to \code{"ID"}.}

\item{REML}{Logical. Use REML? Default \code{FALSE} (required for LRT).}
}
\value{
A list with:
\describe{
  \item{\code{fits}}{Named list of \code{lmerMod} full-model fits.}
  \item{\code{null_fits}}{Named list of null-model fits with the time
    variable dropped.}
}
}
\description{
Fits a Linear Mixed Model with random intercept to each PC score derived
from Stage 1. This function can be called independently if you already
have PC scores.
}
\details{
The model for the \eqn{k}-th PC is:
\deqn{t_{ijk} = \beta_{0k} + \beta_{1k} \cdot j +
  b_{ik} + e_{ijk}.}
}
\examples{
dat <- simulate_lomda_data(seed = 7)
pca_res <- lomda_pca(dat, n_pc = 3)
lmm_res <- lomda_lmm(pca_res$scores, n_pc = 3)
names(lmm_res$fits)

}
