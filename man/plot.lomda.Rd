% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot.lomda}
\alias{plot.lomda}
\title{Plot method for lomda objects}
\usage{
\method{plot}{lomda}(
  x,
  type = "scores",
  pc_x = 1L,
  pc_y = 2L,
  color_by = "time",
  n_top = 10L,
  ...
)
}
\arguments{
\item{x}{A \code{lomda} object.}

\item{type}{Character. One of \code{"scores"} (PC1 vs PC2 scatter),
\code{"loadings"} (feature loadings bar chart), \code{"variance"}
(scree / variance-explained plot), or \code{"trajectory"} (mean PC
score over time). Default \code{"scores"}.}

\item{pc_x}{Integer. PC for the x-axis in score/loading plots. Default 1.}

\item{pc_y}{Integer. PC for the y-axis in score/loading plots. Default 2.}

\item{color_by}{Character. Column name in the score data frame to use for
colouring points (e.g., \code{"time"} or \code{"age"}).
Default \code{"time"}.}

\item{n_top}{Integer. Number of top-loading features to label in the
loadings plot. Default \code{10}.}

\item{...}{Additional arguments passed to underlying plot functions.}
}
\value{
A \code{ggplot} object (invisibly).
}
\description{
Convenience wrapper that dispatches to specialized plot functions. The
\code{type} argument selects which plot to produce.
}
\examples{
dat <- simulate_lomda_data(seed = 42)
fit <- lomda(dat, n_pc = 3)
plot(fit, type = "scores")
plot(fit, type = "variance")
plot(fit, type = "loadings")
plot(fit, type = "trajectory")

}
