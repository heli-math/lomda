% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda.R
\name{predict.lomda_fda}
\alias{predict.lomda_fda}
\title{Predict age-specific PC trajectories from a PCA plus FDA fit}
\usage{
\method{predict}{lomda_fda}(
  object,
  ages = NULL,
  newdata = NULL,
  pcs = NULL,
  type = c("mean", "deviation"),
  ...
)
}
\arguments{
\item{object}{A \code{lomda_fda} object.}

\item{ages}{Numeric vector of ages where the fitted mean PC curves should
be evaluated. If \code{NULL}, the fitted age grid is used.}

\item{newdata}{Optional new omics data. When supplied, PC scores are
predicted with the stored PCA model and returned alongside age-matched
FDA mean scores and deviations.}

\item{pcs}{Integer vector of PCs to return. Defaults to all fitted PCs.}

\item{type}{Character. \code{"mean"} returns the fitted mean curves;
\code{"deviation"} requires \code{newdata} and returns observed PC scores
minus the age-specific fitted mean.}

\item{...}{Ignored.}
}
\value{
A data frame with one row per requested age or new observation.
}
\description{
Predict age-specific PC trajectories from a PCA plus FDA fit
}
\examples{
dat <- simulate_lomda_data(n_subjects = 10, n_features = 8, seed = 3)
fit <- lomda(dat, n_pc = 2, time = "age", method_fda = "spline")
predict(fit, ages = c(35, 45, 55))
predict(fit, newdata = dat[1:3, ], type = "deviation")

}
