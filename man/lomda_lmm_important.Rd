% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importance.R
\name{lomda_lmm_important}
\alias{lomda_lmm_important}
\title{Rank metabolites by LMM-weighted PCA loadings}
\usage{
lomda_lmm_important(x, n_top = 10, pc = NULL, use_abs = TRUE)
}
\arguments{
\item{x}{A \code{lomda} object fitted with \code{time = "visit"}.}

\item{n_top}{Integer. Number of features to return.}

\item{pc}{Integer vector of PCs to use. Defaults to all fitted PCs.}

\item{use_abs}{Logical. If \code{TRUE}, rank by absolute weighted
contributions. Defaults to \code{TRUE}.}
}
\value{
A data frame containing feature names, importance scores, signed
  scores, ranks, and the PC slopes used as weights.
}
\description{
For PCA+LMM fits, metabolite importance is computed across selected PCs as
\deqn{I_r = \sum_k |w_{rk}\hat\beta_{1k}|,}
where \eqn{w_{rk}} is the PCA loading of feature \eqn{r} on PC \eqn{k}, and
\eqn{\hat\beta_{1k}} is the fitted time-effect slope for PC \eqn{k}.
}
\examples{
dat <- simulate_lomda_data(n_subjects = 10, n_features = 8, seed = 2)
fit <- lomda(dat, n_pc = 2, time = "visit")
lomda_lmm_important(fit, n_top = 5)

}
