---
title: "Introduction to lomda: Longitudinal Omics Multivariate Dimension-reduction Analysis"
author: "He Li"
date: "`r Sys.Date()`"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Introduction to lomda}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment  = "#>",
  fig.width  = 7,
  fig.height = 5,
  out.width  = "100%"
)
```

## Overview

The **lomda** package implements a two-stage method for analysing
high-dimensional longitudinal omics data (metabolomics, proteomics,
transcriptomics, etc.) measured at repeated clinical visits.

| Stage | Method | Goal |
|-------|--------|------|
| 1     | PCA    | Reduce dimensionality; extract dominant axes of variation |
| 2     | LMM    | Model PC trajectories over visit; test for time effects |

The model assumed in Stage 2 for the $k$-th PC and subject $i$ at visit $j$:

$$
\boldsymbol{t}_{ik} = \boldsymbol{\beta}_0 + \boldsymbol{\beta}_1 \cdot k + \boldsymbol{b}_i + \boldsymbol{g}_{ik}
$$

where $b_i \sim N(0, \Sigma_b)$ is a subject-specific random intercept and
$\boldsymbol{g}_{ik} \sim N(0, \Sigma_g)$.

---

## Installation

```r
# From GitHub (development version):
# install.packages("devtools")
devtools::install_github("heli-math/lomda")
```

---

## Quickstart

```{r quickstart}
library(lomda)

# 1. Simulate a longitudinal omics dataset
dat <- simulate_lomda_data(
  n_subjects  = 50,
  n_visits    = 3,
  n_features  = 50,
  n_signal    = 5,     # features with a true time trend
  time_effect = 1,
  seed        = 2026
)

cat("Data dimensions:", nrow(dat), "rows x", ncol(dat), "cols\n")
head(dat[, 1:8])
```

---

## Stage 1: PCA

```{r pca}
# Run both stages together
fit <- lomda(dat, n_pc = 3, covariates = "visit")
print(fit)
```

### Variance explained

```{r variance, fig.cap="Scree plot showing variance explained by each PC."}
plot(fit, type = "variance")
```

### PC Score Plot (PC1 vs PC2)

```{r scores, fig.cap="PC1 vs PC2 scores coloured by visit."}
plot(fit, type = "scores", color_by = "visit")
```

### Feature Loadings Biplot

```{r loadings, fig.cap="Biplot of feature loadings on PC1 and PC2."}
plot(fit, type = "loadings", n_top = 8)
```

---

## Stage 2: LMM and Inference

### Summary of fixed effects

```{r summary}
summary(fit)
```

### Likelihood Ratio Test

```{r lrt}
lrt_res <- lomda_lrt(fit)
lrt_res
```

### Wald Test (Satterthwaite df via lmerTest)

```{r wald}
wald_res <- lomda_wald(fit, use_t = TRUE)
wald_res
```

---

## Trajectory Plot

```{r trajectory, fig.cap="Mean PC score trajectory over visits."}
plot(fit, type = "trajectory", pcs = 1:3)
```

---

## Advanced: Running Stages Separately

You can run each stage independently:

```{r separate}
# Stage 1
pca_res <- lomda_pca(dat, n_pc = 5, covariates = "visit")
str(pca_res, max.level = 1)

# Stage 2 (from pre-computed scores)
lmm_res <- lomda_lmm(pca_res$scores, n_pc = 5, covariates = "visit")
names(lmm_res$fits)
```

---

## Session Info

```{r session}
sessionInfo()
```
